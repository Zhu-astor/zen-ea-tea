# 給 opencode 的任務指令 — 第二輪：子頁面、超連結、購物車/收藏/搜尋

## 0. 這是什麼專案

`zen_ea_tea/` 是把 `https://global.ippodo-tea.co.jp/` 整站抓下來（`ippodo_clone/out/`，未改動的原始版本，之後稱**pristine clone**），置換成「蟬吃茶 ZenEaTea」品牌內容後的靜態 POC。純靜態 HTML/CSS/JS，本地用 `python -m http.server 8765` 開。沒有後端、沒有真的購物車 API、沒有 Shopify。

**在動手之前，先讀 `pristine clone`（`ippodo_clone/out/index.html`，用 `python -m http.server 8766` 開來看）。任何你要做的頁面/功能，官網原本長怎樣、行為是什麼，都先去那邊看一遍再動手，不要憑印象猜。**

---

## 1. 上一輪錯在哪裡（這輪必須避免重蹈覆轍）

這些不是空泛的提醒，每一條都是這幾輪抓出來、真實發生過、而且花了大量時間才排查到根因的具體錯誤：

### 1.1 沒有做任何驗證就回報「完成」
上一輪多次宣稱某個區塊修好了，但從頭到尾沒有打開瀏覽器實際看過畫面。結果：Hero 大圖整個是空白的、Feature 輪播圖片大小不一、購物分類小圖示被壓成細條、Footer 整欄垮成單欄貼左——這些全部是**用肉眼一看就能發現**的問題，卻被回報成「已完成」。

**這輪的規則：任何 CSS/HTML 改動，改完後必須實際用 headless browser（Playwright 或同等工具）載入頁面、**捲動過整個頁面**（見 1.6）、截圖或量測 DOM，親眼/親手確認過再回報。不能只憑「程式碼看起來合理」就結案。**

### 1.2 置換圖片時完全沒檢查素材本身是什麼內容、什麼形狀
好幾次把明顯不對的素材塞進不對的版位，例如：
- Hero 大圖用了一張 **812×1426 的直式海報**（整張是滿版文案+留白，不是拍照的橫幅圖），結果撐爆容器只顯示留白區域，畫面整個空白。
- 某個商品卡用了一張 **1462×4376 的行銷海報**（含 logo、直式賞味文案、人物插畫），塞進商品縮圖格，裁切後只看到一截文字。
- 購物分類小圖示（設計上是 90×90 正方形圖示）塞進去的卻是 **1460×835 的橫幅照片**，被壓成細長條完全看不出內容。

**這輪的規則：任何要放進頁面的圖片，動手前先做兩件事：**
1. **打開圖片本人看一眼**（不要只看檔名猜內容）——確認它是「產品照」還是「行銷海報/文案圖」還是別的東西。海報/文案類的圖不能當縮圖用。
2. **量一下實際像素尺寸**（`PIL.Image.open(path).size`），跟目標版位的長寬比對一下，差太多就要嘛用 `object-fit:cover` + `aspect-ratio` 裁切成一致的框、要嘛換一張形狀合理的素材，不要讓瀏覽器用原生比例硬塞。

### 1.3 清理 Shopify 殘留檔案時，靠檔名判斷、沒檢查內容，砍掉了真正需要的東西
`cleanup_scripts.py` 把 `base.css` 和 `style.css` 當成「Shopify 垃圾」一起清掉了，理由只是「看起來像 Shopify 系統檔」。但這兩個檔案其實是 Shopify Dawn 主題**真正的排版基礎**（`.grid`、`.center`、`.page-width` 等全站共用的置中/網格工具類別，還有一條 `:empty{display:none}` 的無障礙重置規則）。砍掉之後，公告列文字置中失效、Footer 四欄選單整個垮成單欄、後來補回 `base.css` 又不小心讓 Hero 大圖被 `:empty{display:none}` 誤刪不見（因為 `style.css` 裡原本有一條 `!important` 規則專門抵銷這個副作用，也一起被砍了，兩個檔案要嘛一起留、要嘛一起砍，不能只砍一個）。

**這輪的規則：要刪除或不連結任何 CSS/JS 檔案前，先搜尋這個檔案裡定義了哪些 class/selector（`grep -o '\.class-name{[^}]*}'`），確認頁面上有沒有元素在用這些 class。只憑檔名「看起來像 Shopify」不是刪除的理由——要嘛引用了真的購物車/結帳 API 端點會失敗，要嘛不是。純 CSS 檔案基本上是安全的，不要因為檔名而砍。**

### 1.4 改一個地方，沒檢查有沒有連帶弄壞別的地方
好幾次修好 A 問題，卻在不知情的狀況下弄壞了 B（例如上面 1.3 提到的 base.css/style.css 案例）。

**這輪的規則：每次改動 CSS/JS 之後，除了驗證你正在修的那個區塊，也要重新檢查前面已經修好的區塊有沒有跟著壞掉（Hero、4 個輪播、Footer、公告列、選購小圖示、三張導覽卡）。寫一支固定的迴歸檢查腳本，每次改完都跑一次，不要只驗證症狀點。**

### 1.5 Swiper 輪播的替代方案沒有覆蓋完整
`bundle.js` 原生初始化 Swiper 在這個離線環境會失敗（原因不明，可能跟 Shopify module stub 缺失有關），前人的做法是額外用 CDN 版 Swiper 手動 `new Swiper(...)` 補一份初始化。這個做法**只補到了「輪播能滑動」這個行為**，但沒發現：
- 同一個模組裡還綁了 Hero 滾動縮放的 jQuery scroll handler，一起漏掉了。
- `.p-top-recipe__list` / `.p-top-iroiro__list` 這兩個元素身兼 Swiper wrapper 跟「JS 沒載入時的備援 CSS Grid」兩職，備援版型（`max-width:940px;margin:0 auto`）真正 Ippodo 官網是靠原生 Swiper 初始化順便解除的，手動補丁沒做這件事，導致輪播卡片被擠壓、第三張卡片位置跑到容器外面被裁掉看不見。

**這輪的規則：任何「手動補一份初始化」的作法，要假設原生初始化除了「開起某個功能」之外，可能還做了其他你看不到的副作用（解除備援樣式、綁定額外事件）。補丁完成後，除了測試主要功能本身，也要對照 pristine clone 檢查同一個元素的 CSS（`display`、`width`、`margin`）有沒有跟預期一致。**

### 1.6 截圖驗證方法本身有陷阱
`.js-fadetarget{opacity:0}` 系列的滾動淡入動畫，靠 `IntersectionObserver` 觸發。Playwright 的 `page.screenshot(full_page=True)` 是**一次性渲染整頁**、不會真的捲動頁面，所以下半頁的淡入元素永遠不會進入「viewport」，永遠停在 `opacity:0`——截圖看起來一大片空白，但其實不是 bug，是截圖方法本身的偽陰性。

**這輪的規則：用 Playwright 驗證任何東西之前，先寫一段「從頁面頂端逐步 `window.scrollTo()` 捲到底、每步等待 100ms 左右」的暖身動作，再做截圖或量測，否則會得到錯誤結論（`zen_ea_tea/screenshot.py` 已經加了這段邏輯，可以直接沿用/參考它的寫法）。**

---

## 2. 這輪的任務範圍

### 2.1 現況：全站目前有這些「假連結」（`<a href="#xxx">` 或 `href="/xxx"`）指向不存在的頁面
從 `index.html` 掃出來的錨點/路徑，數字是出現次數：

**站內錨點（`#xxx`，共通導覽/頁尾常見）：**
`#about`(4) `#store`(4) `#tea-intro`(4) `#tea-bag`(4) `#tea-leaf`(4) `#tea-ware`(4) `#gift-set`(4) `#journal`(10) `#news`(6) `#blog`(3) `#brewing`(3) `#contact`(2) `#faq`(2) `#privacy`(2) `#terms`(2) `#tea-master`(1)

**商品詳情（4 款茶，各自的 `#xxx`）：**
`#honey-black`（蜜香紅茶）`#gaba`（佳葉龍茶）`#fresh-oolong`（鮮翠烏龍）`#yushan-oolong`（玉山烏龍）

**真正指向不存在路由的絕對路徑（會直接 404）：**
`href="/cart"` ×5、`href="/account"` ×3、`href="/pages/wishlist"`、`<form action="/search">`

### 2.2 要做的事

1. **把上面列的錨點連結，改成連到真的頁面**（不是繼續留 `#`）。每個連結對應一個實際存在、內容完整的靜態 HTML 頁面：
   - 4 款茶各自的商品詳情頁（`honey-black.html` 等，或你覺得更合理的檔名結構都可以，但要跟 `<a href>` 一致）——商品圖、名稱、價格、茶葉介紹文字、沖泡建議。內容可以用目前首頁已有的簡短介紹擴寫，如果找不到更多真實素材就明確標示「內容待補」而不是編造不存在的資訊（例如不要編造得獎年份、莊園地址等具體事實）。
   - 「關於蟬吃茶」`#about`、「門市資訊」`#store`、「製茶師陳昭鳳」`#tea-master`、「茶品知識」`#tea-intro`、「沖泡指南」`#brewing`、「常見問答」`#faq`、「聯絡我們」`#contact`、「隱私權政策」`#privacy`、「服務條款」`#terms` 各自一個對應頁面。
   - 「蟬茶日誌」`#journal`／「最新消息」`#news`／`#blog` 這幾個目前對應同一種「文章列表」邏輯，可以合併成一種頁面樣板（部落格列表 + 單篇文章頁），不用每個都重做一次。
   - 「購物」四個分類 `#tea-bag` `#tea-leaf` `#tea-ware` `#gift-set` 各自一個商品列表頁（目前只有 4 款茶，可以先全部列在同一份商品清單上，用分類篩選或直接列出即可，不用假造不存在的商品）。

2. **右上角購物車、收藏、搜尋要有實際行為**，但因為這是純靜態 POC、沒有後端，**不要**做出會打向真實 Shopify API 或任何外部服務的東西：
   - **購物車**：改成純前端、`localStorage` 存資料的假購物車。點擊「加入購物車」把商品加進 `localStorage`，右上角購物車圖示顯示數量，點擊購物車圖示打開一個側邊抽屜（drawer）列出目前加入的商品，可以調整數量/移除。不用做真的結帳流程，「結帳」按鈕可以先導到一個「敬請期待」的靜態頁面或用 alert 提示都行，但要明確、不能是死連結。
   - **收藏 / 願望清單**：同樣 `localStorage`，點擊愛心圖示切換收藏狀態，有一個「我的收藏」頁面列出目前收藏的商品。
   - **搜尋**：這個不需要做真的全文檢索。做一個簡單的**前端關鍵字比對**（比對商品名稱、頁面標題等固定字串陣列），顯示比對到的頁面清單。輸入框要能實際送出、有結果頁，不能是純裝飾。
   - 這三個功能請寫成獨立的小型 vanilla JS 檔案（例如 `assets/zen-cart.js`、`assets/zen-wishlist.js`、`assets/zen-search.js`），不要塞進現有的 `zen-fixes.js`（職責不同，之後要單獨除錯會比較清楚）。

3. **新頁面必須沿用現有的視覺系統**，不要自己發明新的排版風格：
   - 引用跟首頁一樣的 CSS（`base.css`、`main--global.css`、`wl-main--global.css`、`style.css`、`bundle_fix.css`、`zen-fixes.css`，順序不要打亂——這個順序是這幾輪排查出來的正確載入順序，任何顛倒都可能重現前面說過的 bug）。
   - Header／Footer／公告列直接複用首頁那一份 markup，不要重寫一份不一致的版本。
   - 新頁面裡如果用到任何圖片，套用第 1.2 條的檢查流程（先看內容、量尺寸）。

### 2.3 明確不要做的事
- 不要嘗試連接真的 Shopify 後端、真的金流、真的會員系統。這一切都是假的、本地端的。
- 不要為了「看起來像有內容」而編造具體事實（得獎日期、莊園地址、檢驗數字等）。真的沒有素材就留白或寫「內容製作中」，不要編。
- 不要因為要做新頁面，就去動首頁 `index.html` 目前已經修好的區塊（Hero／4 個輪播／Footer／選購小圖示／三張導覽卡）。如果新頁面需要共用 header/footer，用複製貼上而不是抽成共用元件再回頭改首頁的結構（風險不對稱：新頁面壞了影響小，首頁壞了要重查一次）。

---

## 3. 每個頁面/功能做完的驗收標準（不是「寫完程式碼」就算完成）

對每一個新頁面、每一個新功能（購物車/收藏/搜尋），做完後都要能回答：

1. 用 headless browser 實際打開這個頁面，**截圖給我看**，畫面上的東西跟預期一致嗎？
2. 頁面裡所有圖片，有沒有哪張明顯裁切錯誤、被壓扁、比例跑掉？（用前面教的方法量測）
3. Console 有沒有新增的 JS error？（跟修改前的 baseline 錯誤數量比較，不要求歸零，因為原本就有一些殘留的外部追蹤腳本 404，但不能新增)
4. 這個頁面/功能連到的其他連結，點過去是不是也是真的頁面，而不是又是一個死連結？
5. 手機寬度（375px 左右）看起來有沒有明顯跑版？（目前為止全部驗證都只做過桌機寬度，這輪如果有餘力，麻煩至少抽查一下手機版）

完成後列一份清單，每一項照上面 5 點回報結果，不要只寫「已完成」三個字。
